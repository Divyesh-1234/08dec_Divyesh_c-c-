
#include <stdio.h>

int main()
{
    float month, year;
    printf("Enter the number of month:");
    scanf("%f",&month);
    year=month/12;
    printf("Number of the year is:%f",year);
    
    return 0;
}
